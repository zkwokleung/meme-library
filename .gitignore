.DS_Store
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.idea/
*.iml
*.log
build/
coverage/

# Signing material must never be committed, wherever it lands.
*.keystore
*.jks
key.properties

android/.gradle/
ios/Flutter/ephemeral/
ios/Pods/
# The repo tracks no CocoaPods state; `flutter build ios` regenerates it.
ios/Podfile
ios/Podfile.lock
