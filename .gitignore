.DS_Store
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.idea/
*.iml
*.log
build/
coverage/

# Signing material must never be committed, wherever it lands.
*.keystore
*.jks
key.properties

android/.gradle/
ios/Flutter/ephemeral/
ios/Pods/
# The Podfile is tracked because it pins the libwebp pod (WhatsApp sticker
# encoding); generated pod state stays out.
ios/Podfile.lock
